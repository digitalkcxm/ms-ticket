import { app } from '../../src/config/server.js'
import database from "../../src/config/database/database.js";
import logger from "../../src/config/logger.js";
import request from 'supertest'


/*
    TODO: Definir todos os testes com os tickets
 */

describe('Teste de integração dos tickets com as phases', () => {

    it('transfer_massive', async () => {expect(true).toBe(false)})
    it('close massive', async () => {expect(true).toBe(false)})
    it('disable phase com vários tickets', async () => {expect(true).toBe(false)})

})